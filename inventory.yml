all:
  children:
    group1:
      hosts:
        node1:
          ansible_host: 172.21.0.2
          ansible_user: root
          ansible_ssh_private_key_file: ~/.ssh/id_rsa

    group2:
      hosts:
        node2:
          ansible_host: 172.21.0.3
          ansible_user: root
          ansible_ssh_private_key_file: ~/.ssh/id_rsa

    backend:
      hosts:
        node2:

    frontend:
      hosts:
        node1:
