version: '3'

services:
  frontend:
    build:
      context: /app/frontend
      dockerfile: Dockerfile
    container_name: frontend
    ports:
      - "{{ frontend_port }}:80"
    restart: unless-stopped
